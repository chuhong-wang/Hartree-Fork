cmake_minimum_required(VERSION 3.27)

set(This TestExample)

set(Sources ExampleTests.cpp)

add_executable(${This} ${Sources})  

# dependences 
target_link_libraries(${This} PUBLIC  # 
    gtest_main # 
    Molecule)   # what we're testing 

# this is a unit test 
add_test(
    NAME ${This}
    COMMAND ${This}
)
